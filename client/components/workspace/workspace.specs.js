var should = require('chai').should(),
    wrkspace = require('railroad.controller.js'),
    Workspace = wrkspace.Workspace

describe('Workspace', function() {
  it('should have a space object', function() {

  });